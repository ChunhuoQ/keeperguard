# KeeperGuard judge console

Interactive incident replay and verified execution receipt for KeeperGuard.

```bash
npm install
npm run dev
npm test
```

The production build targets OpenAI Sites through the included vinext configuration. The page
contains no secrets and links directly to the public Base Sepolia proof transaction.
